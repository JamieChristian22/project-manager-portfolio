# As-Is Scheduling Process

Patient calls → scheduler checks local calendar → calls department if unclear → manually checks provider → manually checks room → books slot → sends confirmation → changes handled by phone/email → no unified reporting.
